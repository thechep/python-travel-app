# Python Travel Weather App

Simple CLI app that:
- Calls OpenWeather API
- Displays weather info
- Saves search history

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
